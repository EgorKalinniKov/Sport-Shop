<!-- NAVIGATION -->
    <!-- container -->
    <div class="container">
        <!-- responsive-nav -->
        <div id="responsive-nav">
            <!-- NAV -->
            <ul class="main-nav nav navbar-nav">
                <li class=@ViewBag.Index><a href="@Url.Action("Index", "Home")">Home</a></li>
                <li class=@ViewBag.Store><a href="@Url.Action("Store", "Home")">Store</a></li>
                <li class=@ViewBag.Swimming><a href="@Url.Action("Store", "Home", new {category = "Swimming"})">Swimming</a></li>
                <li class=@ViewBag.Basketball><a href="@Url.Action("Store", "Home", new {category = "Basketball"})">Basketball</a></li>
                <li class=@ViewBag.Football><a href="@Url.Action("Store", "Home", new {category = "Football"})">Football</a></li>
                <li class=@ViewBag.Volleyball><a href="@Url.Action("Store", "Home", new {category = "Volleyball"})">Volleyball</a></li>
                <li class=@ViewBag.Gym><a href="@Url.Action("Store", "Home", new {category = "Gym"})">Gym</a></li>
            </ul>
            <!-- /NAV -->
        </div>
        <!-- /responsive-nav -->
    </div>
    <!-- /container -->
<!-- /NAVIGATION -->